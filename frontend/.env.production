# 生产环境：前端与后端同源部署（Nginx 把 /api 反代到 Flask:5000）。
# 不要写 http://127.0.0.1:5000 —— 构建产物里的地址在用户浏览器执行，
# 127.0.0.1 指的是访问者自己的电脑，公网用户会直接请求失败。
# 若后端独立域名部署，改成 https://api.你的域名/api 并在后端收敛 CORS_ORIGINS。
VITE_APP_TITLE = 高校学生行为数据采集与可视化系统
VITE_API_BASE_URL = /api
